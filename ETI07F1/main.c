#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;  //liczba namiotow
    int green, yellow;
    scanf("%d%d%d", &n, &green, &yellow);


    for(int i = 0; i < n; i++)
    {

    }

    free(green);
    free(yellow);
    return 0;
}
